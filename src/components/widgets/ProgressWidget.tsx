import { createSignal } from "solid-js";
// todo: #4 Progress Widget- Only show anything if user is enrolled else show link to enroll
// todo: #5 progress Widget - Fetch data from the progress store
// todo: #6 progress Widget - Calculate number of completed content by types, how many are in progress
// todo: #7 Progress widget -  a simple daisy chart to show percentage completed
// todo: #8 Progress Widget - show the first in-progress content as current

interface Props{
    userId: string;
    isEnrolled: boolean;
}

export default function ProgressWidget (props: Props){
    
    return null;

}